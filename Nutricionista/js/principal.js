let principal = document.querySelector("h1");

principal.textContent = "Aparecida Nutricionista";

let clientes = document.querySelectorAll("#tabela-pacientes, .paciente");

atualizaTabela(clientes);
